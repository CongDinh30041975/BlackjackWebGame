import { register, login } from "../../lib/supabase/auth";

function LoginForm() {
    return (
        <div className="register-container">
            <p>Đây là trang đăng ký</p>
        </div>
    );
};

export default LoginForm;